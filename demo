2 modul
-----------------------------------------------------------------------------------
2 zadanie на hq-srv
#mdadm --create /dev/md0 –l 0 –n 2 /dev/sdb /dev/sdc
#mdadm --detail --scan --verbose | tee -a /etc/mdadm.conf
#mkfs.ext4 /dev/md0
#go to /etc/fstab, pishi: /dev/md0    /raid    ext4    defaults    0    0
#mkdir /raid
#mount -av
# checkakaem lsblk

3 Zadanie na hq-srv
#apt-get install nfs-server -y
#mkdir /raid/nfs
#chmod -R 777 /raid/nfs
#go to /etc/exports and pishi /raid/nfs 192.168.200.0/28(rw)
#exportfs -arv
#systemctl enable --now nfs-server
#go to HQ-CLI
#mkdir /mnt/nfs
#chmod -R 777 /mnt/nfs
#v fail /etc/fstab new strochka
#192.168.100.2:/raid/nfs /mnt/nfs nfs defaults 0 0
#rebootni tak srabotaet
#df -h

4 zadanie na isp
#apt-get install chrony -y
#go to fail /etc/chrony.conf and local stratum 5 and zakomentit #pool pool.ntp.org iburst 
#new strochka: server ntp0.ntp-servers.net iburst prefer minstratum 4 and eta nizhe allow 0.0.0.0/0
#systemctl restart chronyd
#na vsex mashinax apt-get update && apt-get install chrony -y
#redachim /etc/chrony.conf  and zakomentit #pool pool.ntp.org iburst 
# new strochka server 172.16.1.1 iburst          dlya mashin BR-RTR BR-SRV servak budet 172.16.2.1!!!!!
#systemctl restart chronyd
# проверка chronyc sources

5 задание 
#NA SERVAKE BR-SRV!!!!
#apt-get update && apt-get install –y ansible sshpass
#Potom v file /etc/ansible/hosts
#br-rtr ansible_ssh_host=172.16.2.2 ansible_ssh_user=net_admin ansible_ssh_pass=P@ssword
#hq-rtr ansible_ssh_host=172.16.1.2 ansible_ssh_user=net_admin ansible_ssh_pass=P@ssword
#hq-srv ansible_ssh_host=192.168.100.2 ansible_ssh_user=sshuser ansible_ssh_pass=P@ssword ansible_ssh_port=2026
#hq-cli ansible_ssh_host=192.168.200.2 ansible_ssh_user=user ansible_ssh_pass=resu
#potom idem v /etc/ansible/ansible.cfg
##host_key_checking = False >> host_key_checking = False karoch typo raskomentit
# kak proverit? ansible all -m ping 
# Esli dofua red texta idi i restart na vsex mashunax kotorie ti pisal vishe 

11 zadanie 
#apt-get install yandex-browser


### 1 zadanie samba
#_______________________________________BR-SRV__________________________________________

apt-get update && apt-get install task-samba-dc -y

rm -rf /etc/samba/smb.conf

mkdir -p /var/lib/samba/sysvol

echo search au-team.irpo >> /etc/net/ifaces/ens18/resolv.conf
echo nameserver 127.0.0.1 >> /etc/net/ifaces/ens18/resolv.conf   
samba-tool domain provision

Enter(x5) > P@ssw0rd(x2)

cp /var/lib/samba/private/krb5.conf /etc/krb5.conf

systemctl enable --now samba

samba-tool group add hq
for i in {1..5}; do
> samba-tool user add hquser$i P@ssw0rd;
> samba-tool user setexpery hquser$i --noexpiry;
> samba-tool group addmembers hq hquser$i;
done

если при создании пользователей не будет никаких ошибок то вы все прописали правильно
проверить можно с помощью команд:

	samba-tool group listmembers hq

сначала получают билет через kinit, а затем проверяют его наличие с помощью klist

	kinit Administrator
	klist

#_______________________________________HQ-RTR__________________________________________

vim /etc/dhcp/dhcpd.conf

# See dhcpd.conf(5) for further configuration

ddns-update-style none;

subnet 192.168.200.0 netmask 255.255.255.240 {
    option routers                  192.168.200.1;
    option subnet-mask              255.255.255.240;

    option nis-domain               "domain.org";
    option domain-name              "au-team.irpo";
   -option domain-name-servers      192.168.100.2;
   +option domain-name-servers      192.168.3.2; 

    range dynamic-bootp 192.168.200.2 192.168.200.7;
    default-lease-time 21600;
    max-lease-time 43200;
}

systemctl restart dhcpd

#_______________________________________HQ-CLI__________________________________________

systemctl restart network

Далее:

	Заходим на HQ-CLI --> Меню(снизу слева) --> Раздел Приложения --> Администрирование --> Центр управления системой --> Раздел Пользователи --> 	Аутентификация

	Выбираем пункт Домен Active Directory
	
	Вводим au-team.irpo в поле Домен

	Листаем вниз и нажимаем применить

далее нужно прописать права для пользователей группы hq

	roleadd hq wheel

далее заходим в файл по пути nano /etc/sudoers.d/hq

вставляем в этот файл

	Cmnd_Alias SHELLCMD = /bin/cat, /bin/grep, /usr/bin/id  
  
	WHEEL_USERS ALL=(ALL:ALL) SHELLCMD

-----------------------------------------------------------------------------------
6 zadanie 
#apt-get install -y docker-engine docker-compose-v2
#systemctl enable --now docker
#mount /dev/sr0 /mnt/
#docker load < /mnt/
#docker load < /mnt/docker/site_latest.tar
##docker load < /mnt/docker/mariadb_latest.tar
#docker images
#Теперь создаём файл compose.yaml и редачим его:
#services:
  database:
    container_name: db
    image: mariadb:10.11
    restart: always
    ports:
      - "3306:3306"
    environment:
      MARIADB_DATABASE: testdb
      MARIADB_USER: test
      MARIADB_PASSWORD: P@ssw0rd
      MARIADB_ROOT_PASSWORD: root
#на пользователях ДЭ пароль будет другой учитывайте это а не слепо копируйте
  app:
    container_name: tespapp
#название контейнера правильное, в задании написано, что основной контейнер testapp должен называться tespapp
#при дальнейшей настройки docker это может ввести вас в заблуждение
    image: site:latest
    restart: always
    ports:
      - "8080:8000"
    environment:
      DB_TYPE: maria
      DB_HOST: 192.168.3.2
      DB_PORT: "3306"
      DB_NAME: testdb
      DB_USER: test
      DB_PASS: P@ssw0rd
    depends_on:
      - database
# docker compose up -d
# проверям на сайте http://192.168.3.2:8080


7 Zadanie
#apt-get install lamp-server -y
#mount /dev/sr0 /mnt/
#cp /mnt/web/index.php /var/www/html/
#cp /mnt/web/logo.png /var/www/html/
#$servername = "localhost";
#$username = "webc";
#$password = "P@ssw0rd";
#$dbname = "webdb";
#systemctl enable --now mariadb
#mariadb -u root
#В появившемся поочередно прописываем:
#
#CREATE DATABASE webdb CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

#CREATE USER 'webc'@'localhost' IDENTIFIED BY 'P@ssw0rd';

#GRANT ALL PRIVILEGES ON webdb.* TO 'webc'@'localhost';

#FLUSH PRIVILEGES;

#EXIT;
#systemctl enable --now httpd2
#mariadb -u webc -p webdb < /mnt/web/dump.sql
